# This file contains chart value overrides for the Helm output unit test
volumes:
  sharedHome:
    nfsPermissionFixer:
      enabled: true

bamboo:
  license:
    secretName: "testSecret"
    licenseKey: "testKey"
