# This template is first processed by `sed` as Kustomize doesn't support variable replacement.
# Our use case is a dynamic build ID that is generated by Bamboo and it is then used to identify the deployments belonging to one build

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patchesStrategicMerge:
  - nfs-server-resource-requests.yaml

nameSuffix: -CI_PLAN_ID
commonLabels:
  build.identifier: CI_PLAN_ID
  role: CI_PLAN_ID-nfs-server
