{{- if .Values.monitoring.grafana.createDashboards }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-indexing-dashboard
  labels:
    {{- include "common.labels.commonLabels" . | nindent 4 }}
    {{- with .Values.monitoring.grafana.dashboardLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
data:
  {{ include "common.names.fullname" . }}-{{ .Release.Namespace}}-indexing.json: |-
{{ .Files.Get "grafana-dashboards/indexing.json" | indent 4 }}
{{- end }}